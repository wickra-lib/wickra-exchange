% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exchange.R
\name{wkex_ws_place_order_full}
\alias{wkex_ws_place_order_full}
\title{Place a full order over the WebSocket order API.}
\usage{
wkex_ws_place_order_full(
  wse,
  market,
  side,
  quantity,
  price = NA_real_,
  stop_price = NA_real_,
  time_in_force = "gtc",
  client_order_id = NULL,
  reduce_only = FALSE,
  post_only = FALSE,
  stp = "none",
  quantity_text = NA_character_,
  price_text = NA_character_,
  stop_price_text = NA_character_
)
}
\arguments{
\item{wse}{A `wickra_ws_execution` object.}

\item{market}{Market string.}

\item{side}{"buy" or "sell".}

\item{quantity}{Order quantity.}

\item{price}{Limit price, or `NA` for a market order.}

\item{stop_price}{Trigger price, or `NA` for a non-trigger order.}

\item{time_in_force}{"gtc" (the default), "ioc" or "fok".}

\item{client_order_id}{An id of your choosing, or `NULL`.}

\item{reduce_only}{Close-only: the order may not increase a position.}

\item{post_only}{Maker-only: cancelled rather than crossing the spread.}

\item{stp}{"none" (the default), "expire_maker", "expire_taker" or
"expire_both".}

\item{quantity_text}{Exact quantity as a string, or `NA` to use `quantity`.

  R's numeric is a double and holds about fifteen significant digits, where
  this library holds every order number in an exact decimal. Written as a
  number, `12345678.90123456789` becomes `12345678.90123457` -- a different
  order, placed without a word. A string reaches the venue as written.}

\item{price_text}{Exact limit price as a string, or `NA` to use `price`.}

\item{stop_price_text}{Exact trigger price as a string, or `NA` to use
`stop_price`.}
}
\value{
The resulting order as a list.
}
\description{
The [wkex_place_order()] form of [wkex_ws_place_order()], for the same reason
the REST path has one: the narrow call cannot carry a trigger price, a
time-in-force, or any of the flags that decide what the order actually is.
}
