% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exchange.R
\name{wkex_place_batch_full}
\alias{wkex_place_batch_full}
\title{Place several full orders in one request.}
\usage{
wkex_place_batch_full(
  adv,
  markets,
  sides,
  quantities,
  prices = NA_real_,
  stop_prices = NA_real_,
  times_in_force = "gtc",
  client_order_ids = NULL,
  reduce_onlys = FALSE,
  post_onlys = FALSE,
  stps = "none",
  quantity_texts = NA_character_,
  price_texts = NA_character_,
  stop_price_texts = NA_character_
)
}
\arguments{
\item{adv}{A `wickra_advanced` object.}

\item{markets}{Character vector of market symbols.}

\item{sides}{Character or integer vector of sides ("buy"/"sell").}

\item{quantities}{Numeric vector of quantities.}

\item{prices}{Numeric vector of limit prices (`NA` for a market order).}

\item{stop_prices}{Numeric vector of trigger prices (`NA` for none).}

\item{times_in_force}{Character vector: "gtc", "ioc" or "fok".}

\item{client_order_ids}{Character vector of ids, or `NULL` for none.}

\item{reduce_onlys}{Logical vector: close-only.}

\item{post_onlys}{Logical vector: maker-only.}

\item{stps}{Character vector: "none", "expire_maker", "expire_taker" or
"expire_both".}

\item{quantity_texts}{Character vector of exact quantities, or `NA` per
element to use the numeric beside it. R's numeric is a double and holds
about fifteen significant digits; a string reaches the venue as written.}

\item{price_texts}{Character vector of exact limit prices, or `NA`.}

\item{stop_price_texts}{Character vector of exact trigger prices, or `NA`.}
}
\value{
A list of results, each `list(order = , error = )`: `order` on success
  or `error` (an integer status code) on a per-order rejection.
}
\description{
[wkex_place_batch()] can say only market, side, quantity and price, so a
batched order from R could never be a stop-loss, an immediate-or-cancel or a
post-only, however carefully the venue clients carried them. This takes the
whole request, as parallel vectors: one element of each per order.
}
\details{
Scalars are recycled to the number of markets, so the common case of "same
time-in-force for every leg" needs one value rather than a repeated vector.
}
